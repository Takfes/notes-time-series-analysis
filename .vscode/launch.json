{
  "configurations": [
    {
      "name": "Python: Current File + Smart CWD",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}"
    }
  ]
}
